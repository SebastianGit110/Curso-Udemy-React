// Arreglos

// const array = new Array(10); // Esta sintaxis no es recomendada. Solo si se quiere tener un array con tamaño definido

const array = [];

array[0] = 0;
array.push(1);

console.log(array);
